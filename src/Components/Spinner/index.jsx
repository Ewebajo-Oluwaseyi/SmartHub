import logo from "../../assests/Iphone-spinner-2.gif"
import {Loader} from './Spinner'

const Spinner = () => {
    return (
        <Loader>
            <img src={logo} alt="" />
        </Loader>
    )
}

export default Spinner
